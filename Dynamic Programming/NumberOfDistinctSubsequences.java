//https://practice.geeksforgeeks.org/problems/number-of-distinct-subsequences0909/1?utm_source=youtube&utm_medium=collab_striver_ytdescription&utm_campaign=number-of-distinct-subsequences0909
import java.util.*;
public class NumberOfDistinctSubsequences {
    int distinctSubsequences(String s) {
        int n = s.length();
        int dp[] = new int[n];
        Arrays.fill(dp,-1);
        HashMap<Integer,HashSet<String>> map = new HashMap<>();
//        HashSet<String> res = find(0,n,dp,map);
        return 1;
    }
}
